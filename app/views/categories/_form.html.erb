<%= simple_form_for(@category) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :order %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
